# 验收报告 · 蜘蛛纸牌牌面素材同步（2026-09-10）

## 需求
“D:\双姝扑克牌设计\完整54张 对照所给文件，更新蜘蛛纸牌中纸牌有改变的部分。”

## 比对方法
按原转换流水线（`scripts/prepare_spider_cards.py`：PNG → 等比缩到宽 320 → JPEG q82）逐个重新转换
源目录 54 张 + 上级目录两张牌背（`10_牌背_蓝副.png` / `11_牌背_红副.png`），
与 `assets/spider/cards/` 现有 56 个文件做逐字节 + 像素差异比对。

## 比对结果（共 56 项）
| 结果 | 数量 | 明细 |
| --- | --- | --- |
| 完全一致（无需更新） | 53 | 含 `黑桃A`：源文件被重新保存过（mtime 09-10 21:43）但转换结果与本地逐字节相同，内容无变化 |
| **有变化** | 3 | **红桃6**（像素差异 75.6%）、**红桃J**（93.2%）、**黑桃J**（63.6%） |

## 处理
1. 3 张新图**用同一流水线重新转换**后替换（宽 320 / JPEG q82，与其它牌一致）：
   `红桃6.jpg 36,209B`、`红桃J.jpg 50,100B`、`黑桃J.jpg 32,295B`
2. 替换前校验：花色颜色特征正确（红桃偏红 0.251/0.271，黑桃偏黑 0.000）、
   与其它 52 张**无重复图**（md5 比对）
3. 已同步到本机安装目录（三张图 md5 与项目一致）

## 验证证据（真实浏览器）
- 52 张牌图**全部加载成功**（0 失败），新图尺寸均 320×480
- 渲染棋盘：54 张 `<img>` **0 张失败**（44 张牌背 + 10 张明牌）
- HTTP 校验：三张新图经 URL 编码请求均返回 200，字节数与本地一致

## 同步
- git：提交 `assets/spider/cards/红桃6.jpg`、`红桃J.jpg`、`黑桃J.jpg`
- 本机安装目录已更新（桌面程序刷新即生效）
- **未重新打包安装包/Release**：本轮只更新素材文件；如需把新牌面做进安装包分发，可再发一版
  （v1.2.1），届时 `build_release.py` 会自动带上并同步本机
