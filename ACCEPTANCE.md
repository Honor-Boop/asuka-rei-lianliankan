# 验收报告 · 新增 9×9 数独玩法（2026-09-10）

## 需求
“添加新小游戏，格数为 9*9 的数独。”

## 交付
新增 `sudoku.html`（9×9 数独）+ `index.html` 玩法大厅入口，风格/装饰/结算与既有玩法一致。

### 盘面生成（关键正确性）
- 回溯 + 随机候选序生成**完整合法解**；随机挖洞，每挖一格用 MRV 计数求解器校验**唯一解**，
  不满足则回填。三档难度按保留提示格数：
  見習い訓練 44 / 正規隊員 34 / 使徒級 27（提示次数 5 / 3 / 1）。

### 交互与功能
- 点选格子：选中格 + 同行/同列/同宫（peer）+ 全盘同数（same）高亮
- 输入：键盘 1-9、方向键移动、Delete/Backspace/0 擦除；下方数字键盘（右下角显示该数字剩余个数）
- 候选数模式（✎ 按钮 / N 键）：填入小字备选，9 宫格 3×3 渲染，再点取消
- 即错提醒（默认开，可关）：填错立即标红并累计「失误」
- 提示（💡 / H 键）：填出一格正确答案，次数有限并显示剩余
- 撤销（↩ / Ctrl+Z，快照式，最多 300 步）、擦除、重开、玩法选择、❓ 玩法说明弹窗
- 计时；各难度最佳用时存 localStorage（eva_sudoku_best_<diff>）
- 通关：81 格全填满且与唯一解一致 → 「PATTERN : BLUE」横幅 + 贴底结算卡；
  零失误零提示有额外评价；结算不遮挡背景（沿用贴底横幅样式）

## 验证证据

### 1. Node 回归测试 sudoku-test.js（5/5 通过）
- 完整盘面 20 次生成：每行/每列/每宫 1-9 各一次 全部合法
- 三档挖洞：提示格数精确达标（44/34/27），唯一解校验通过，题面与解无矛盾，生成 ≤2ms
- 已知唯一解经典盘面（30 提示）解计数 = 1（求解器正确性）

### 2. 页面内自测 window.__sudokuSelfTest()（浏览器实测）
- fullOk/uniqueOk/givenOk 全 true；sample = trainee 44、agent 34、angel 27 全部达标
- DOM：81 格棋盘、11 个键盘按钮、难度标签「正規隊員」、开局 34 个固定格

### 3. 玩法链路 13/13 通过（浏览器，真实页面状态）
点选高亮 / 填入正确值 / peer+same 高亮 / 填错标红 / 失误计数=1 / 撤销还原 /
候选数写入 / 候选 3×3 渲染 / 提示消耗 1 次 / 提示填出正确答案 / 通关判定 /
结算弹窗显示 / 结算文案

### 4. 真实鼠标+键盘
点击空格 → sel 正确且仅 1 个高亮；按数字键填入正确值且不误判错；
按 N 切换候选模式生效；按 Delete 擦除生效。

### 5. 大厅路由
index.html 玩法大厅出现「数独 · 9×9」入口，点击后跳转 sudoku.html（标题「数独 · 双姝数阵」）。

## 同步
- git：commit eb2159f（sudoku.html / sudoku-test.js / index.html / README.md）
- GitHub：已推送 main（commit d6cf7b97af）
- 本地正式安装目录 AppData\Local\EVA小游戏\EVA小游戏 已同步 sudoku.html / index.html（桌面入口即可玩到）
- 未重建安装包与 Release（本轮未发版；如需对外分发数独，下一步重建发版即可）
