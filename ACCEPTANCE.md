# 验收报告 · 宽屏下正文与两侧装饰栏重叠修复（2026-09-10）

## 需求
“缩小游戏区域以及上面功能按钮，不要与左右两边的图片重叠。”

## 根因
`deco.js` 在视口 ≥1300px 时显示左右各 **252px** 的 EVA 装饰栏；
而各游戏页正文最宽 1240px 且水平居中 → 1600px 屏幕上
1240 + 2×252 = 1744 > 1600，棋盘与顶部功能按钮便压在装饰栏上。

## 修复
1. **deco.js**：装饰栏出现时给 `body` 加左右内边距（`--deco-gutter`，260px），
   正文自动居中到两侧栏之间的区域；同时导出 `window.__decoGutter` 与 `decochange` 事件，
   供页面按视口计算像素尺寸时扣减。
2. **mine / gomoku / index / sudoku**：把 `window.innerWidth` 的算尺寸逻辑改为
   扣除 `2 × window.__decoGutter`，棋盘/格子按可用宽度重算（真正的“缩小游戏区域”）。
3. **mine.html**：99×99 这类超大棋盘因格子有 12px 下限，宽度固定 1298px，
   给 `#boardWrap` 加 `max-width:100% + overflow-x:auto`，改为**容器内横向滚动**，
   彻底不越界（1920px 下可完整显示、无需滚动）。

## 验证证据（真实浏览器，逐项实测）

### 1. 三种宽度 × 五个页面，越界检测
检测对象：`header`（顶部功能按钮区）、`.controls`、`#table` / `#grid` / `#board`、
`#boardWrap`、`#pad`、`footer`；判定标准：矩形完全落在 `[栏宽, 视口-栏宽]` 内。

| 视口 | 内容可用区 | 结果 |
| --- | --- | --- |
| 1400px | [252, 1148] | 5 个页面 × 4~6 个元素，**越界 0 项** |
| 1680px | [252, 1428] | 同上 **越界 0 项** |
| 1920px | [252, 1668] | 同上 **越界 0 项** |

### 2. 尺寸确实随让位缩小（1400px）
可用宽度 880px：扫雷棋盘 697、五珠 671、连连看 761、数独 518 —— 均在可用区内。

### 3. 运行中跨断点重排（1280 → 1500）
装饰栏出现（deco-on=true，gutter=260），棋盘自动重排居中于 [402, 1099]，
处于 [252, 1248] 内，**不越界**。

### 4. 极端棋盘（扫雷 99×99）
| 视口 | 容器宽 | 棋盘宽 | 容器越界 | 容器内可滚动 |
| --- | --- | --- | --- | --- |
| 1280 | 1264 | 1298 | 否 | 是 |
| 1400 | 880 | 1298 | 否 | 是 |
| 1680 | 1160 | 1298 | 否 | 是 |
| 1920 | 1313 | 1298 | 否 | 否（完整显示） |

页面整体横向滚动：均为否（不会出现整页横滚）。

## 同步
- 修改已复制到本机安装目录（桌面程序刷新即生效）
- git commit da9a8ff，已推送 main（ee990ff3dc）
- 未重新打包安装包/Release（如需把此修复做进安装包，可再发一版）
