# 验收报告 · 新增「拼图」与「接龙」玩法（2026-09-10）

## 需求
“先做拼图和接龙”（承接上一轮的推荐）；并补充要求：
“拼图每次随机挑选图库里的图片，在更新图库后，新的图片也会被选取为拼图”。

## 交付
| 页面 | 玩法 |
| --- | --- |
| `puzzle.html` | 图库壁纸碎片复原：3×3 / 4×4 / 5×5 三档 |
| `klondike.html` | 标准 Klondike 接龙：7 列 + 4 基础叠（A→K）+ 牌垛 + 弃牌堆 |

`index.html` 玩法大厅新增两个入口（含配色与路由），README 同步。

### 拼图：实时图库（用户追加要求）
- 素材由 `canvas` 裁成正方形（center-crop / cover），任意比例壁纸都不变形
- 每次开新局、每次「换图」都通过 **`/api/list` 实时拉取图库清单**（serve.py 每次扫盘生成），
  不依赖页面加载时的 manifest.js
- 另外每 30 秒、以及切回页面时自动感知图库变化：新增会提示，当前图被删会提示换图

### 接龙：规则实现
- 列内红黑交替降序叠放、空列只放 K、基础叠同花 A→K、牌垛抽牌与循环
- 三档难度：見習い抽 1 / 正規隊員抽 3 / 使徒級抽 3 且只能循环一轮
- 双击送基础叠、⤴ 自动收牌、💡 提示、↩ 撤销、计时与步数记录

## 修复的两个真实交互 bug（测试中发现）
1. 选中一张牌后再点目标列里的**牌**，会变成「重新选中」而不是落下 → 改为先判落下再判选中
   （点目标列的牌或列内空白都能落）
2. `render()` 给弃牌堆挂事件时把下标传到了 `a` 位（`hookCard(type, a, b)` 需要 `b`），
   导致**双击送基础叠、弃牌拖动永远失效** → 传参修正
3. 附带改进：选中高亮改为只切换 class（不再重建 DOM），避免第二次点击落在新元素上、
   导致 `dblclick` 与拖拽事件丢失

## 验证证据

### 1. 拼图
- 自测：三档 9/16/25 格、排列合法、错位达标、交换/撤销/提示归位/通关判定全部通过
- **图库实时性端到端**（真实上传→不刷新页面）：
  · 上传测试图后清单 39 → **40**，新图 `wallpapers/rei_upload_01.jpg` 在清单中
  · 随机抽图 300 次可抽到新图（3 次，符合 1/40 期望）
  · 用新图开局：素材标签「绫波丽 · 900x900」、预览与碎片底图均来自该图
  · 删除该图后清单回到 39、被删图不再出现、页面提示「当前壁纸已从图库移除，点「换图 ▶」换一张」
  · 测试图已删除，用户图库保持原样 39 张
- 交互：拖动交换（0↔5 位置互换）、点击交换、撤销各 1 步均正确，步数计数正确

### 2. 接龙
- 自测：发牌 1..7 列/28 张在手/24 张牌垛、仅列底翻开、52 张无重复；
  红黑交替降序、空列只放 K、基础叠 A 起同花顺接（含花色不符与跳级拒绝）、整段识别、撤销、胜利判定
- 操作链路（逐步实测）：点选高亮 → 点目标列落牌（黑7 上叠红6）→ 双击红 A 收进基础叠 →
  点牌垛抽 3 张 → 弃牌顶牌可拖 → 红 6 放空列被拒并提示 → 撤销回退 1 步
- 拖拽（真实 DragEvent）：dragstart 设置选中、drop 落牌成功，步数 +1

### 3. 大厅路由
八个玩法入口齐全（连连看/五珠/观景/蜘蛛/扫雷/数独/**拼图**/**接龙**），
点击「接龙」跳转 klondike.html（标题「接龙 · 双姝扑克」）正常。

### 4. 回归测试（全部通过）
- `puzzle-test.js` **8/8**（打乱合法性、充分打乱、交换可逆、任意排列可复原、提示归位、通关、实时清单与离线回退、随机不重复）
- `klondike-test.js` **8/8**（发牌结构、叠放规则、基础叠规则、整段识别、抽牌含边界、循环含使徒級限轮、
  **3000 步随机走法不变量**：总牌数恒 52 / 无重复 / 基础叠同花连续 / 列内红黑交替降序、胜利判定）
- 既有回归无回归：`spider-rule-test.js` 11/11、`sudoku-test.js` 5/5

## 同步
- git commit aaa9038，已推送 main（36a58e8a9f）
- 两个新页面与测试脚本已复制到本机安装目录（桌面程序刷新即生效）
- **未重打包安装包/Release**：本轮为新增玩法开发；如需对外分发（v1.3.0），可再发一版
