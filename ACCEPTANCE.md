# 验收报告 · 蜘蛛纸牌微软规则对齐（2026-09-09）

## 需求（来自用户确认与 B 站参考视频对齐）

1. 拖 8 到 9 所在列后，8 叠在目标牌下面、露出上缘（列 9 8，微软观感）
2. 接牌只能落到目标列最底部的牌之下（不能插到列中间）
3. 收套只认同花 K(顶)→A(底) 降序完整 13 张

## 代码改动（spider.html，commit ede2738）

| 项 | 改前 | 改后 |
|---|---|---|
| 可拖段识别 | 同花连续 ±1 双向（升序也认） | 仅列顶起同花**降序**连续（9 8 7、K Q J 10） |
| 接牌判定 | 列内任意明牌 = 段**底**+1，可插中间 | 落点 = 目标列最底翻开牌，点数 = 段**顶**+1 |
| 盖牌 | 逻辑不区分 | 永不成为落点；明链尾接牌时插在盖堆之前，盖牌保持盖着 |
| 收套 | 升/降序双向、顶不必是 K | 仅同花 K→A 单向 13 张 |
| 帮助/状态栏文案 | 旧语义 | 同步“段顶+1 / 只能列底 / K 顶 A 底” |

## 验收证据

### 1. Node 规则回归测试（spider-rule-test.js，12/12 通过）
- segLen：987=3、KQJ10=4、升序 789=1（不整拖）、98X 断点=2
- canPlace：987→10 允许（段顶 9+1=10）；987→8 拒绝；单张 8→9 允许；8→10 拒绝；8→[9明,盖] 允许、盖牌保持盖着；8→[9,7,盖] 拒绝（只能接列底明，9 虽满足但不许插中间）
- doMove 落位：[10,9,8,7]；8 落 [9,8,盖] 插在 9 后盖前
- collectAll：K→A 收 +1 并清列；A→K 不收；Q..A 12 张不收

### 2. 页面内断言（15/15 通过，经本地服务器加载真实 spider.html 执行）
覆盖同上 + 4 花色开局 54 张、列顶明其余盖。

### 3. 真实鼠标交互（Playwright 点击链路）
确定性局面 [9] 列 + [10] 列：点 9 顶卡 → 选中段 0/1；点 10 列 → 移动成功（步数 1，列 1 = 10u,9u，UI 渲染 2 张卡，选中态清除）。渲染方向未改动（此前已验收），本次仅规则收紧。

## 遗留说明

- 发牌位置（unshift 列顶）与渲染层（z-index、34px/12px/6px 间距）保持原样，未在本次需求范围内改动。
- 桌面安装包按用户“不用同步更新安装包”约束未重建。
