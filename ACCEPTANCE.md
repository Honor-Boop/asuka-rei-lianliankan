# 验收报告 · 蜘蛛纸牌「8 不能移动」修复（2026-09-10）

## 用户反馈
“图片中牌的顺序是 8→7→8，而且 8 不能移动，绑定在 87 上。”

## 排查结论（先排除误判）
1. **牌堆与素材无误**：52 张牌面两两互不相同（md5 + 感知哈希），颜色特征与花色一致
   （红桃/方块偏红、梅花/黑桃偏黑），无重复、无错标。
2. **8→7→8 是合法状态**：7 可以合法落在 8 之下（build-down），第三张 8 来自「发牌」
   （发牌给每列底部补的是任意点数的明牌）。此时最下面那张 8 就是唯一可动牌。
3. **真实拖拽本身可用**：用坐标级真实鼠标拖拽（cua.drag）验证，把最下面那张 8 拖到空列成功。
4. **真正的缺陷**：`render()` 里只有**可拖段最上面那一张**被设为 `draggable`，
   而那张牌恰恰被下面的牌压住、只露出约 26px；用户去抓段的中间/下面那些「看起来更好抓」
   的牌时，浏览器根本不会发起拖拽，表现为「这张 8 拖不动 / 像黏在 87 上」。

## 修复（spider.html）
- **整段任意一张都能作为拖拽抓手**：`i >= start` 的翻开牌全部设 `draggable` + `dragstart`
  （抓哪张都移动同一整段，与微软一致）。
- **视觉提示**：
  · 可拖动段的牌 → `cursor: grab`（按下时 grabbing）+ 悬停时绿色描边高亮
  · 被压住不能动的明牌 → `cursor: not-allowed`，一眼看出「这张动不了，因为上面压着牌」
- 未改动规则判定（segStart / canPlace / doMove / collectAll 均未变）；未改模式与花色相关逻辑。

## 验证证据（开发服务 8768，实测）
1. 列 [9,8,7]（同花）：三张**全部可拖**（改前只有第一张）
2. 列 [8,7,8]：上两张标注「被压/禁止」，最下面 8 可拖
3. **真实鼠标从段中间那张 8 抓起 → 整段移动**：列0 清空、列1 变为 `10,9,8,7`（moves=1）
4. 用户场景实测：[8,7,8] 的最下面 8 真实拖拽到空列 → 列变 `8,7` / `8`（moves=1）
5. 点击选中未被破坏：异花列 [9♦,8♥] 点末尾 8 → sel=0/1、高亮 1 张
6. 规则回归 `spider-rule-test.js`：11/11 通过
7. 代码标记核对：segStart/canPlace/doMove/collectAll/dealRow 均在位

## 同步
- 修复文件已复制到本机安装目录（AppData\Local\EVA小游戏\EVA小游戏\spider.html），
  桌面程序刷新页面即可生效；下次 `build_release.py` 会随分发包正式带上
- 临时开发服务脚本 `_dev_serve.py` 已删除（8768 服务仍在运行，供快速预览）
