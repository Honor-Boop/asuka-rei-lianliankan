# 验收报告 · 蜘蛛纸牌列方向对齐微软标准（2026-09-09，commit cd52782）

## 需求
用户对照微软蜘蛛参考视频后反馈“还不对”。经以微软风格开源实现 fSpider
（github.com/fuzzley/fSpider，TableauPile.arrangeCards / setLastCardFaceUp / drawFromStockPile）
为基准核对，确认原实现**整列上下颠倒**：

| 维度 | 原实现（错） | 微软标准（fSpider 基准，已改） |
|---|---|---|
| 开局列 | 明牌在数组头/视觉顶部，盖牌压底 | 盖牌簇在顶部（7px 细距），**最下方一张翻开** |
| 可拖段 | 从列顶（数组头） | 从列底（数组尾）逆扫同花降序 |
| 翻牌 | 翻数组头 | 源列明链抽空后翻数组尾（原位） |
| 发牌 | unshift 列顶 | push 列尾（每列底部 +1 明牌） |
| 落点 | 列尾明牌 = 段顶+1（判定本身已对） | 同左，整段 push 列尾 |
| 渲染 | 明牌在上 34px/12px/6px | 间距由前一张决定：盖 7px / 明 26px；z 随数组递增，列底明牌完整可见；虚线框仅空列显示 |

## 验收证据

1. **规则回归测试**（spider-rule-test.js，11/11 通过）：
   段识别（盖簇压顶不影响 987 段=3、异花断开=2、升序不可拖=1、KQJ10=4）；
   落点（987→[盖,10] 允许、→8 拒绝、盖尾列拒绝、空列允许）；
   移动落位（[盖3,10,9,8,7]、源列盖自动翻开成明牌、空列收段保序）；
   发牌 push 列尾；收套（K 上→A 下 13 张、升序/12 张不收、盖簇+完整 K→A 可收）。
2. **页面断言 5/5**：新局 6/5 张结构、尾张翻开、渲染 tops/z 顺序（盖簇在上、底部明牌 z 最高）、
   canPlace 落点、带盖簇收套。
3. **真实点击链路**（DOM 事件同 onClick）：点 9 选中 987 段（sel 3 张）→ 点 10 列 →
   落位 [盖3,10u,9u,8u,7u]，源列盖 2 原位翻开；渲染 tops=[0,7,33,59,85]、z=101..104 与 fSpider 一致。

## 同步说明
- 发现 scripts/api_push.py 为早期遗留脚本，硬编码只推 index/gomoku/README/DEVELOPMENT，
  spider.html 历次修改均未上远端；新增参数化 scripts/api_push_files.py 补推。
- 远端 spider.html blob 与本地一致（contents API sha c44451de28），提交 8a3097adcc。
- 桌面安装包按用户“不用同步更新安装包”约束未重建。
