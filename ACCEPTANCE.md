# 验收报告 · 图库功能扩展到各游戏页（2026-09-10）

## 需求
“将查看图库的功能扩展到其他几个游戏” —— 此前仅 index.html 页脚有「🖼 壁纸图库」链接，
其余游戏页（五珠/扫雷/蜘蛛/数独/观景）没有图库入口。

## 交付
新增自包含组件 `gallery_panel.js`，接入 6 个页面：index / gomoku / mine / spider / sudoku / view。

### 组件能力
- **入口**：自动往页面控制区（`.controls`，观景页为 `#bar`）追加「🖼 图库」按钮，沿用各页按钮样式；
  无控制区时退化为右下角浮钮；另支持 **G 键**快捷键打开（输入框内不触发）
- **就地浏览**（不离开当前对局）：
  · 角色筛选条：全部 39 / 绫波丽 7 / 明日香 10 / 美里 22（带张数）
  · 缩略图墙（用 thumbs 缩略图，懒加载），当前背景那张标注「当前背景」并高亮
  · 点图放大灯箱：←/→ 键与滚轮翻页、Esc 关闭、显示角色/分辨率/序号
- **设为背景**：点按调用 background.js 新增的 `__bgShowSrc(src)`，立即把该壁纸换成当前背景，
  刷新「当前背景」标记并轻提示（随后继续自动轮换）
- **打开完整图库**：跳 gallery.html 做上传/删除等管理
- **键盘隔离**：面板/灯箱打开时独占键盘，避免与页面自身快捷键冲突（观景页 ←/→ 换壁纸）

### 依赖改动
- `background.js`：新增 `__bgCurrentItem()` 与 `__bgShowSrc(srcOrIndex)`（跳到指定壁纸）

## 验证证据（浏览器实测）

### 1. 数独页功能链 9/9
按钮注入到 .controls、背景 API 就绪（列表 39 张）、浮层打开、缩略图墙 39 张渲染、
当前背景标记唯一、角色筛选条（全部 39 | 绫波丽 7 | 明日香 10 | 美里 22）、
筛选绫波丽后 7 张全为该角色、灯箱打开（绫波丽 · 2621x1855 · 1/7）、→ 翻页生效

### 2. 设为背景与快捷键 5/5
设为背景生效（misato_upload_03.jpg → asuka_wall_03.jpg）、当前背景标记刷新、
轻提示出现、Esc 逐层关闭（灯箱→浮层）、G 键打开图库

### 3. 六页入口逐页检查（全部通过）
index / gomoku / mine / spider / sudoku → 按钮位于 `.controls`；view → 位于 `#bar`；
每页点击后浮层均成功打开且渲染 39 张缩略图

### 4. 观景页按键隔离 6/6
灯箱打开时按 →：灯箱翻页生效、背景下标保持 26 不变（未被观景页快捷键抢走）；
Esc 关灯箱 → Esc 关浮层；关闭后观景页 ← 恢复正常换壁纸（26→25）

## 同步
- git：commit a9c28bd（9 文件）
- 本地正式安装目录 AppData\Local\EVA小游戏\EVA小游戏 已同步（桌面入口即可用）
- README 增加 gallery_panel.js 说明与接入页面清单
