# 明日香&凌波丽连连看

以《新世纪福音战士》明日香与绫波丽为主题的**连连看**小游戏，外加一个 **2K 壁纸图库**，
壁纸会作为页面的自动轮换背景。纯静态页面，无任何依赖，克隆后双击 `index.html` 即可游玩。

| 连连看 | 壁纸图库 |
| --- | --- |
| ![游戏截图](docs/screenshot-game.png) | ![图库截图](docs/screenshot-gallery.png) |

## 页面

| 文件 | 说明 |
| --- | --- |
| `index.html` | 连连看游戏。三档难度（見習い 6×4 / 正規隊員 8×6 / 使徒級 10×8），28 种牌面（两位角色各 16/12 张），开局先选难度；倒计时制、消除 +3 秒；使徒級为 8 局连续关卡，第 2 局起棋子定向位移 |
| `gallery.html` | 壁纸图库。按角色筛选、灯箱大图浏览（←/→ 翻页、Esc 关闭）、下载原图 |
| `background.js` | 两页共用的背景轮换组件：每 12 秒淡入淡出切换一张壁纸，提供 `🖼 换背景` 按钮（`window.__bgNext()`） |
| `deco.js` | EVA 元素侧边装饰栏：NERV 徽记、MAGI、A.T. FIELD、同步率面板（联动战况，连击暴走、通关 400%） |
| `start_lianliankan.vbs` | Windows 桌面启动器：自动拉起本地服务并以应用窗口打开游戏 |
| `wallpapers/` | 25 张 2K（2560×1440）以上壁纸（绫波丽 8 / 明日香 7 / 美里 10）+ 缩略图 + `manifest.js` 清单 |
| `images/` | 连连看牌面用的 160×160 头像图（28 张） |
| `assets/` | 桌面快捷方式图标（绫波丽 × 明日香 合成） |
| `scripts/` | 图片抓取与处理脚本（Python + Pillow） |
| `DEVELOPMENT.md` | 完整开发时间线 |

## 本地运行

直接双击 `index.html` 即可游玩（纯静态、离线可用）；
或起一个静态服务器后访问 <http://127.0.0.1:8765/index.html>：

```bash
python -m http.server 8765
```

### 桌面端启动（Windows）

双击 `start_lianliankan.vbs`（或在桌面创建指向它的快捷方式，图标用 `assets/icon.ico`）：
自动检测并在后台拉起本地服务（`python -m http.server 8765`），
然后以**应用窗口模式**（无地址栏）在 Edge/Chrome 中打开游戏，体验接近桌面程序。

## 游戏规则与功能

- 开局先选择难度：**見習い訓練**（6×4 · 12 种图案 · 90 秒）、**正規隊員**（8×6 · 24 种图案 · 210 秒）、**使徒級**（10×8 · 20 种图案 · 8 局连续 · 每局 150 秒）；
- 点击两张**相同图片**的牌，若能用**不超过 2 个拐点**的线连通（可以绕棋盘外圈）即消除，消除 **+3 秒**；选错直接取消选中；
- 连击：5 秒内连续消除，得分翻倍（+10 → +20 → …）；
- 使徒級第 2 局起，棋子每 10 秒按 右→下→左→上 轮换方向环绕位移（选中跟随、位移后保底可解），过关 +100 分，8 局全通触发"人类补完"通关特效；
- 💡 提示 ×3、🔀 洗牌（无可用配对时自动洗牌）、↻ 重开、⚙ 难度、📊 战绩（最近 60 局）、🖼 换背景、🔊 音效开关；
- 各难度最佳用时与战绩本地保存（localStorage）。

## 维护壁纸库

重新抓取/扩充壁纸：

```bash
python scripts/fetch_wallpapers.py   # 默认每角色下载 10 张候选，人工筛选后删除不要的
python scripts/fetch_wallpapers.py   # 再跑一次可重建 thumbs 与 manifest.js
```

壁纸来源为 safebooru 公开 API（safe 分级、宽高 ≥ 2560×1440、按站点评分排序）。
图片仅供个人欣赏，版权归原作者所有。
